[project]
name = "agx-torch"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "agx-core",
    "torch>=2.11.0",
    "torchvision>=0.26.0",
    "ultralytics>=8.4.37",
]

[tool.uv.sources]
agx-core = { path = "../../packages/agx-core", editable = true }